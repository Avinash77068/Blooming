import PageShell from "../common/PageShell";

export default function Workshops() {
  return (
    <PageShell
      subtitle="Programs"
      title="Workshops"
      description="Hands-on sessions to build practical skills in design, robotics, and storytelling."
    >
      <p className="text-left text-sm text-slate-500 dark:text-slate-300">
        Each workshop blends guided discovery with creative experimentation, so
        students can practice skills that extend beyond textbooks.
      </p>
    </PageShell>
  );
}
